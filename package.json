{
  "name": "paws-and-play-network",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Paws & Play Network — a pet-sitter directory built from the Claude Design handoff. Deployable to Cloudflare Pages.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler pages deploy dist",
    "deploy:worker": "wrangler deploy"
  },
  "dependencies": {
    "qrcode.react": "^4.2.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.10",
    "wrangler": "^3.86.0"
  }
}
